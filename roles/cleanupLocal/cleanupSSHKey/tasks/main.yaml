##############################################################################
# Remove materialized SSH key files from disk
#
# USAGE:
#   Run as the final role in a localhost cleanup play after all remote
#   plays have completed. Removes key.prv and key.pub written by
#   materializeSSHKey so they do not persist after the playbook run.
#
# REQUIRED VARIABLES:
#   - taskDir: Path to tasks directory (from common role)
#   - dataDir: Path to data directory (from common role)
#
# EXAMPLE:
#   - name: Cleanup materialized SSH keys
#     hosts:        localhost
#     connection:   local
#     gather_facts: false
#     roles:
#       - cleanupSSHKey
#
# Copyright 2026 Douglas WF Acheson (dwfa@dwfa.ca)
# Licensed under Apache License 2.0. See LICENSE.md for details.
#
# Version: 1.0
# Date: March 22, 2026
##############################################################################
---                                            # identifies a YAML config file

- name: Remove materialized SSH keys
  ansible.builtin.include_tasks:
    file: "{{ taskDir }}/users/cleanupSSHKey.yaml"